# MSSQL





```
文本型
当需要存储大量的字符数据，如较长的备注、日志信息等时，字符型数据最长8000个字符的限制可能使它们不能满足这种应用需求，此时可使用文本型数据。

文本型包括text和ntext两类，分别对应ASCII字符和Unicode字符。

text类型可以表示最大长度为2^31-1(2G)个字符，其数据的存储长度为实际字符数字节。
ntext类型可表示最大长度为2^30-1个Unicode字符，其数据的存储长度是实际字符个数的两倍（以字节为单位）。
```

